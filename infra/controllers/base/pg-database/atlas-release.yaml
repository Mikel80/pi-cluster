apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: atlas-operator
  namespace: pipis-db
spec:
  interval: 1h
  chart:
    spec:
      chart: atlas-operator
      version: "0.7.x"
      sourceRef:
        kind: HelmRepository
        name: atlas-operator-charts
        namespace: flux-system
      interval: 10m